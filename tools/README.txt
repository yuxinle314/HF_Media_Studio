Place ffmpeg.exe here if you want server-side video transcoding.

Expected simple path:
  tools\ffmpeg.exe

You may also extract a Windows ffmpeg build under this folder. The server
will search for tools\...\ffmpeg.exe automatically.

Do not commit ffmpeg.exe or extracted binary folders to Git.
